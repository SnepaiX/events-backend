# Проведение мероприятий

Тестовое задание для позиции Backend Python Intern.

## Стэк
- Python
- Django
- Django REST Framework
- drf-spectacular

## Локальный запуск
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
